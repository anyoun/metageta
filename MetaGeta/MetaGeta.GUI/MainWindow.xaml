<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mgds="clr-namespace:MetaGeta.DataStore;assembly=MetaGeta.DataStore"
    x:Class="MainWindow" x:Name="MainWindow" Title="MetaGeta"
    Height="651" Width="932" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:System="clr-namespace:System;assembly=mscorlib" xmlns:GUI="clr-namespace:MetaGeta.GUI" xmlns:Utilities="clr-namespace:MetaGeta.Utilities;assembly=MetaGeta.Utilities" mc:Ignorable="d">

    <Window.Resources>
        <DataTemplate DataType="{x:Type mgds:MGDataStore}">
            <TextBlock Text="{Binding Path=Name}"/>
        </DataTemplate>

        <Style TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="True" />
        </Style>

        <HierarchicalDataTemplate DataType="{x:Type GUI:NavigationTab}"
                                  ItemsSource="{Binding SubTabs}">
            <StackPanel Orientation="Horizontal" Margin="7,0,0,0">
                <Image Source="{Binding Path=Icon}" Width="16" Height="16" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                <TextBlock Text="{Binding Path=Caption}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </StackPanel>

        </HierarchicalDataTemplate>

        <DataTemplate DataType="{x:Type GUI:SeparatorNavigationTab}">
            <Line SnapsToDevicePixels="True" Stretch="Fill" X1="0" X2="1" Stroke="Black" Width="100" />
        </DataTemplate>

        <Utilities:AutoRefreshCollectionViewSource x:Key="NavigationTabsGrouped" Source="{Binding Path=NavigationManager.NavigationTabs, ElementName=MainWindow}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Group"/>
            </CollectionViewSource.GroupDescriptions>
        </Utilities:AutoRefreshCollectionViewSource>

        <DataTemplate x:Key="HeaderTemplate">
            <StackPanel Margin="2" Orientation="Horizontal">
                <Image Source="pack://application:,,,/MetaGeta.GUI;component/Resources/db.png" Width="16" Height="16" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Margin="5,0" VerticalAlignment="Center" />
                <Rectangle Height="1" Fill="Blue" VerticalAlignment="Center" />
            </StackPanel>
        </DataTemplate>

    </Window.Resources>

    <DockPanel x:Name="DockPanel1" >
        <Grid x:Name="LeftHandGrid" DockPanel.Dock="Left" Width="200">
            <ListBox x:Name="lbDataStores" Margin="6,6,6,34" 
			    ItemsSource="{Binding Source={StaticResource NavigationTabsGrouped}}">
                <ListBox.GroupStyle>
                    <GroupStyle HeaderTemplate="{StaticResource HeaderTemplate}"/>
                </ListBox.GroupStyle>
                <ListBox.Resources>
                    <LinearGradientBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" StartPoint="0,0" EndPoint="0,1" >
                        <GradientStop Color="#CDDFEB" Offset="0"/>
                        <GradientStop Color="#90BADA" Offset=".49"/>
                        <GradientStop Color="#80AACA" Offset=".5"/>
                        <GradientStop Color="#80AACA" Offset="1"/>                        
                    </LinearGradientBrush>
                </ListBox.Resources>
            </ListBox>

            <Button x:Name="btnNewDataStore" Margin="6,0,0,6" VerticalAlignment="Bottom" HorizontalAlignment="Left" Height="24" Width="24" Content="+" FontWeight="Bold" FontSize="14"/>
        </Grid>

        <ContentControl x:Name="CurrentViewContentControl" Content="{Binding Path=CurrentView, ElementName=MainWindow, Mode=Default}"/>
    </DockPanel>
</Window>
